const
    card_variant = {
        default: { rotateX: '0deg' },
        rotated: { rotateX: '45deg' }
    },
    character_variant = {
        default: { opacity: 0, translateY: '0%' },
        rotated: { opacity: 1, translateY: '-20%' }
    };

export { card_variant, character_variant};